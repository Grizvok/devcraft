import React from 'react';

export default () => (
  <section className="hero is-large has-bg-img3">
    <div className="hero-head contact-hero">
    </div>
    <div className="hero-body"></div>
  </section>
);